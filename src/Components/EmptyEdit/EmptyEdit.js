import React from "react";

const EmptyEdit = () => {
  return (
    <div className="container mx-auto text-center mt-12">
      <span className="text-gray-500 text-2xl uppercase">
        select a contact from <span className="text-red-500">contacts</span> page
      </span>
    </div>
  );
};

export default EmptyEdit;
